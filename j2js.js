function aria(test) {
    
    var r = f.t.value * 1;
    const p = 3.14;
    if (test == 1)
        f.t2.value = p * Math.pow(r, 2);
    
  
}
function circumference(test2) {
    var r = f.t.value * 1;
    const p = 3.14;
    if (test2 == 2)
        f.t2.value = 2 * p * r;
    
    
}